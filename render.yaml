services:
  - type: web
    name: rl-memory-backend
    env: python
    buildCommand: pip install -r requirements.txt && python -m spacy download en_core_web_sm
    startCommand: python run.py
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_DEBUG
        value: false
      - key: GROQ_API_KEY
        sync: false
      - key: FRONTEND_URL
        value: "*"
